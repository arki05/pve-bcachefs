NAME=bcachefs
# Three devices for the multi-device filesystem plus one held back for a
# second, quota-less filesystem that the raw-image fallback needs. Several of
# the plugin's options - data-replicas, the targets, erasure coding - are
# untestable on a single device.
DISKS=4
TESTS=../tests
SOURCE=../..
